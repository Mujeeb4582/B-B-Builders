const Amenities = () => {
  // const { data } = useQuery(AMENITIES_QUERY);
  // const amenities = data?.amenities || [];

  return (
    <div>
      <h1>Amenities</h1>
      {/* <ul>
        {amenities.map((amenity) => (
          <li key={amenity.id}>{amenity.name}</li>
        ))}
      </ul> */}
    </div>
  );
};

export default Amenities;
